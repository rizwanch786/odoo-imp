# # generate states from 1, 20 for odoo selection field
# a = [(f'a{i}', i) for i in range(1,21)]
# print(a)

# # generate years only
# y = [(f'y{i}',{i}) for i in range(2000, 2050)]
# print(y)

# # generate days only
# d = [(f'd{i}',{i}) for i in range(1,32)]
# print(d)

# # Generate months
# m = [(f'm{i}', {i}) for i in range(1,13)] 
# print(m)
